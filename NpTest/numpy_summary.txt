广播机制:
1、对于矩阵或者向量和数字的广播，则是直接将对应的数字和矩阵或者向量的每一个元素计算即可
2、对于向量和矩阵的广播，我是否可以这样理解，如果是一维向量和矩阵进行广播，那么就是先将一维向量扩展为（1，n），将其扩展为两个矩阵进行比较，然后走第三步流程
3、对于两个高维矩阵进行广播的情况，则是低维度矩阵从右往左匹配高维矩阵，如果某个维度不满足相等或者为1，则广播失败，如果匹配完成，则针对低维缺失的位置直接补1